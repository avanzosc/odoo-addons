<odoo>
    <record id="sale_subscription_view_form" model="ir.ui.view">
        <field name="model">sale.subscription</field>
        <field name="inherit_id" ref="sale_subscription.sale_subscription_view_form" />
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Penalties">
                    <field name="subscription_penalty_ids">
                        <tree editable="bottom">
                            <field name="penalty_type_id" widget="many2one" />
                            <field name="applied_date" />
                        </tree>
                    </field>
                </page>
            </notebook>
        </field>
    </record>

    <menuitem
    id="subscription_operation_menu"
    name="Subscriptions"
    parent="account_penalty.penalty_operation_menu"
    action="sale_subscription.sale_subscription_action"
    sequence="40"
  />
</odoo>
