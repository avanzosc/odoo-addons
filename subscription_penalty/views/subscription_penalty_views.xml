<odoo>
    <record id="view_subscription_penalty_tree" model="ir.ui.view">
        <field name="model">subscription.penalty</field>
        <field name="arch" type="xml">
            <tree>
                <field name="subscription_id" widget="many2one" />
                <field name="penalty_type_id" widget="many2one" />
                <field name="applied_date" />
            </tree>
        </field>
    </record>

    <record id="view_subscription_penalty_form" model="ir.ui.view">
        <field name="model">subscription.penalty</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="subscription_id" />
                        <field name="penalty_type_id" />
                        <field name="applied_date" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_subscription_penalty_search" model="ir.ui.view">
        <field name="model">subscription.penalty</field>
        <field eval="10" name="priority" />
        <field name="arch" type="xml">
            <search string="Subscription Penalties">
                <field name="subscription_id" />
                <field name="penalty_type_id" />
                <field name="applied_date" />
                <group expand='0' string='Group by...'>
                    <filter
            string='Subscription'
            name="subscription"
            context="{'group_by': 'subscription_id'}"
          />
                    <filter
            string='Penalty Type'
            name="penaltytype"
            context="{'group_by': 'penalty_type_id'}"
          />
                    <filter
            string='Applied Date'
            name="applieddate"
            context="{'group_by': 'applied_date'}"
          />
                </group>
            </search>
        </field>
    </record>

    <record id="action_subscription_penalty" model="ir.actions.act_window">
        <field name="name">Subscription Penalties</field>
        <field name="res_model">subscription.penalty</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
    </record>

    <menuitem
    id="menu_subscription_penalty"
    name="Subscription Penalties"
    parent="sale_subscription.menu_sale_subscription"
    action="action_subscription_penalty"
    sequence="50"
  />

  <menuitem
    id="subscription_penalty_operation_menu"
    name="Subscription Penalties"
    parent="account_penalty.penalty_operation_menu"
    action="action_subscription_penalty"
    sequence="30"
  />

</odoo>
