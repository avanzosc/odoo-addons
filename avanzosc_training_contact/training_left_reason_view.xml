<?xml version="1.0" encoding="UTF-8"?>
<openerp>
    <data>

	<!--
		========================================================================
		training.left.reason
		========================================================================
	-->

	<!-- TREE -->
	<record model="ir.ui.view" id="training_left_reason_tree_view">
		<field name="name">training.left.reason.tree.view</field>
		<field name="model">training.left.reason</field>
		<field name="type">tree</field>
		<field name="arch" type="xml">
			<tree string="Identification Type">
				<field name="left_reason_id" />
				<field name="left_reason_name" />
			</tree>
		</field>
	</record>

	<!-- FORM -->
	<record model="ir.ui.view" id="training_left_reason_from_view">
		<field name="name">training.left.reason.form.view</field>
		<field name="model">training.left.reason</field>
		<field name="type">form</field>
		<field name="arch" type="xml">
			<form string="Identification Type">
				<field name="left_reason_id" />
				<field name="left_reason_name" />
			</form>
		</field>
	</record>

	<!-- SEARCH -->
    <record model="ir.ui.view" id="training_left_reason_search_view">
		<field name="name">training.left.reason.search.view</field>
		<field name="model">training.left.reason</field>
		<field name="type">search</field>
		<field name="arch" type="xml">
			<search string="Identification Type">
				<field name="left_reason_id" />
				<field name="left_reason_name" />
			</search>
		</field>
	</record>
    
    </data>
</openerp>