<?xml version="1.0" encoding="UTF-8"?>
<openerp>
    <data>

	<!--
		========================================================================
		TRAINING.CITY
		========================================================================
	-->

	<!-- TREE -->
	<record model="ir.ui.view" id="training_city_tree_view">
		<field name="name">training.city.tree.view</field>
		<field name="model">training.city</field>
		<field name="type">tree</field>
		<field name="arch" type="xml">
			<tree string="Identification Type">
				<field name="city_name" />
				<field name="city_code" />
				<field name="provincia" />
			</tree>
		</field>
	</record>

	<!-- FORM -->
	<record model="ir.ui.view" id="training_city_from_view">
		<field name="name">training.city.form.view</field>
		<field name="model">training.city</field>
		<field name="type">form</field>
		<field name="arch" type="xml">
			<form string="Identification Type">
				<field name="city_name" />
				<field name="city_code" />
				<field name="provincia" />
			</form>
		</field>
	</record>

	<!-- SEARCH -->
    <record model="ir.ui.view" id="training_city_search_view">
		<field name="name">training.city.search.view</field>
		<field name="model">training.city</field>
		<field name="type">search</field>
		<field name="arch" type="xml">
			<search string="Identification Type">
				<field name="city_name" />
				<field name="city_code" />
				<field name="provincia" />
			</search>
		</field>
	</record>
    
    </data>
</openerp>