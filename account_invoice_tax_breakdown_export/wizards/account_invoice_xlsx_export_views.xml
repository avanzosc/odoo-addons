<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="account_invoice_breakdown_export_view_form" model="ir.ui.view">
        <field name="model">account.invoice.breakdown.export</field>
        <field name="arch" type="xml">
            <form string="Export Invoices with Tax Breakdown">
                <sheet>
                    <div class="row container">
                        <div class="mb-12">
                            <h3 class="font-weight-bold">Invoices XLSX file</h3>
                            <div class="text-justify px-0">
                                This will create a XLSX file with a list of invoices.
                            </div>
                        </div>
                    </div>
                </sheet>
                <field name="invoice_ids" invisible="1" />
                <footer>
                    <button
            string='Export'
            name="export_invoices"
            type="object"
            class="btn-primary"
          />
                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
           </form>
        </field>
    </record>

    <record id="account_invoice_breakdown_export_action" model="ir.actions.act_window">
        <field name="name">Export Invoices with Tax Breakdown</field>
        <field name="res_model">account.invoice.breakdown.export</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="account_invoice_breakdown_export_view_form" />
        <field name="target">new</field>
        <field name="groups_id" eval="[(4, ref('account.group_account_invoice'))]" />
        <field name="binding_model_id" ref="account.model_account_move" />
        <field name="binding_view_types">list</field>
    </record>
</odoo>
