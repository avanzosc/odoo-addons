<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="my_surveys_action" model="ir.actions.act_window">
        <field name="name">My Surveys</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">survey.survey</field>
<!--        <field name="domain">[('responsible_user_ids','in',user.id)]</field>-->
        <field name="view_mode">kanban,tree,form</field>
    </record>

   <!-- <record id="my_surveys_action" model="ir.actions.server">
        <field name="name">My Surveys</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="survey.model_survey_survey" />
        <field name="binding_model_id" ref="survey.model_survey_survey"/>
        <field name="state">code</field>
        <field name="code">
            action=self.get_filtered_surveys(context=context)
&lt;!&ndash;            action=self.get_filtered_surveys(cr, uid, context.get('active_ids', []), context=context)&ndash;&gt;
        </field>
    </record>-->
<!--        action="action_survey_user_input"-->
    <menuitem
        action="my_surveys_action"
        id="action_menu_my_surveys"
        parent="menu_my_surveys"
        name="My Surveys"/>
    <menuitem
        id="menu_my_surveys"
        name="My Surveys"
        web_icon="slide_channel_survey,static/description/icon.png"
        groups="base.group_user"
        sequence="70"/>
</odoo>
