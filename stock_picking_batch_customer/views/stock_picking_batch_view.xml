<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="stock_picking_batch_form" model="ir.ui.view">
        <field name="model">stock.picking.batch</field>
        <field name="inherit_id" ref="stock_picking_batch.stock_picking_batch_form" />
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="customer_id" />
            </field>
            <field name="picking_ids" position="attributes">
                <attribute
          name="domain"
        >[('partner_id', '=', customer_id) if customer_id else (1, '=', 1)]</attribute>
            </field>
            <field name="move_line_ids" position="attributes">
                <attribute
          name="domain"
        >[('picking_partner_id', '=', customer_id) if customer_id else (1, '=', 1)]</attribute>
            </field>
            <field name="move_ids" position="attributes">
                <attribute
          name="domain"
        >[('picking_partner_id', '=', customer_id) if customer_id else (1, '=', 1)]</attribute>
            </field>
        </field>
    </record>

    <record id="stock_picking_batch_tree" model="ir.ui.view">
        <field name="model">stock.picking.batch</field>
        <field name="inherit_id" ref="stock_picking_batch.stock_picking_batch_tree" />
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="customer_id" optional="show" />
            </field>
        </field>
    </record>

    <record id="stock_picking_batch_filter" model="ir.ui.view">
        <field name="model">stock.picking.batch</field>
        <field name="inherit_id" ref="stock_picking_batch.stock_picking_batch_filter" />
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="customer_id" />
            </field>
            <filter name="user" position="after">
                <filter
          string="Customer"
          name="customer"
          domain="[]"
          context="{'group_by': 'customer_id'}"
        />
            </filter>
        </field>
    </record>
</odoo>
