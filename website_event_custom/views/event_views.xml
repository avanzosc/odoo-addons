<?xml version="1.0" encoding="utf-8"?>

<odoo>
        <record model="ir.ui.view" id="view_event_form">
            <field name="model">event.event</field>
            <field name="inherit_id" ref="event.view_event_form"/>
            <field name="arch" type="xml">
                <field name="tag_ids" position="after">
                    <field name="resource_calendar_id"/>
                    <field name="days" />
                </field>
                <field name="user_id" position="after">
                    <field name="first_teacher_id"/>
                    <field name="second_teacher_id"/>
                </field>
            </field>
        </record>

        <record model="ir.ui.view" id="view_event_tree">
            <field name="model">event.event</field>
            <field name="inherit_id" ref="event.view_event_tree"/>
            <field name="arch" type="xml">
                <field name="user_id" position="after">
                    <field name="first_teacher_id"/>
                    <field name="second_teacher_id"/>
                </field>
            </field>
        </record>

    <record id="view_event_event_search" model="ir.ui.view">
        <field name="model">event.event</field>
        <field name="inherit_id" ref="event.view_event_search"/>
        <field name="arch" type="xml">
            <field name="user_id" position="after" >
                <field name="first_teacher_id"/>
                <field name="second_teacher_id"/>
            </field>
            <filter name="responsible" position="after">
                <filter string="First Teacher" name="firstteacher" context="{'group_by': 'first_teacher_id'}"/>
                <filter string="Second Teacher" name="secondteacher" context="{'group_by': 'second_teacher_id'}"/>
            </filter>
        </field>
    </record>
</odoo>
