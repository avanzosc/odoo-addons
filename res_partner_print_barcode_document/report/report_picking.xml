<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="report_picking" inherit_id="stock.report_picking" priority="100">
        <div name="right_box" position="attributes">
            <attribute name="t-if">o.partner_id.print_barcode</attribute>
        </div>
        <th name="th_barcode" position="replace" />
        <th name="th_product" position="after">
            <th
        name="th_barcode"
        class="text-center"
        t-if="has_barcode and o.partner_id.print_barcode"
      >
                <strong>Product Barcode</strong>
            </th>
        </th>
        <td t-if="has_barcode" position="replace" />
        <xpath expr="//span[@t-field='ml.product_uom_qty']/.." position="before">
            <td class="text-center" t-if="has_barcode and o.partner_id.print_barcode">
                <t t-if="product_barcode != move.product_id.barcode">
                    <span t-if="move.product_id and move.product_id.barcode">
                        <img
              t-if="len(move.product_id.barcode) == 13"
              t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s&amp;quiet=%s' % ('EAN13', move.product_id.barcode, 400, 100, 0)"
              style="height:35px"
              alt="Barcode"
            />
                        <img
              t-elif="len(move.product_id.barcode) == 8"
              t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s&amp;quiet=%s' % ('EAN8', move.product_id.barcode, 400, 100, 0)"
              style="height:35px"
              alt="Barcode"
            />
                        <img
              t-else=""
              t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s&amp;quiet=%s' % ('Code128', move.product_id.barcode, 400, 100, 0)"
              style="height:35px"
              alt="Barcode"
            />
                        <br />
                        <span t-field="move.product_id.barcode" />
                    </span>
                    <t t-set="product_barcode" t-value="move.product_id.barcode" />
                </t>
            </td>
        </xpath>
    </template>
</odoo>
