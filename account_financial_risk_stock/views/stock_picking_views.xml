<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="vpicktree" model="ir.ui.view">
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.vpicktree" />
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="risk_exception" readonly="1" optional="hide" />
            </field>
        </field>
    </record>

    <record id="view_picking_internal_search" model="ir.ui.view">
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_internal_search" />
        <field name="arch" type="xml">
            <field name="move_line_ids" position="after">
                <field name="risk_exception" />
            </field>
            <filter name="activities_exception" position="after">
                <separator />
                <filter
          string="With Risk Exception"
          name="with-risk"
          domain="[('risk_exception', '=', True)]"
        />
                <filter
          string="Without Risk Exception"
          name="without-risk"
          domain="[('risk_exception', '=', False)]"
        />
            </filter>
        </field>
    </record>
</odoo>
