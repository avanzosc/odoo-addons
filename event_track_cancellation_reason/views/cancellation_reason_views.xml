<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <record id="view_cancellation_reason_tree" model="ir.ui.view">
        <field name="name">cancellation.reason.tree</field>
        <field name="model">cancellation.reason</field>
        <field name="arch" type="xml">
            <tree string="Cancellation Reason" editable="bottom">
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record id="action_cancellation_reason" model="ir.actions.act_window">
         <field name="name">Cancellation Reason</field>
         <field name="type">ir.actions.act_window</field>
         <field name="view_mode">tree</field>
         <field name="res_model">cancellation.reason</field>
         <field name="domain">[]</field>
         <field name="context">{}</field>
    </record>

    <menuitem id="menu_cancellation_reason"
        action="action_cancellation_reason"
        name="Cancellation Reason" parent="event.menu_event_configuration"
        sequence="35"/>

</odoo>
