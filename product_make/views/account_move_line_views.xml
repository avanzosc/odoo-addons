<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="view_move_line_form" model="ir.ui.view">
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_form" />
        <field name="arch" type="xml">
            <field name="analytic_distribution" position="before">
                <field name="make_ids" widget="many2many_tags" />
                <field name="allowed_make_ids" widget="many2many_tags" invisible="1" />
                <field name="make_id" domain="[('id','in',allowed_make_ids)]" />
            </field>
        </field>
    </record>

    <record id="view_move_line_tree" model="ir.ui.view">
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_tree" />
        <field name="arch" type="xml">
            <field name="account_id" position="after">
                <field name="make_ids" widget="many2many_tags" optional="show" />
                <field name="make_id" optional="show" />
                <field name="allowed_make_ids" widget="many2many_tags" invisible="1" />
            </field>
        </field>
    </record>

    <record id="view_account_move_line_filter" model="ir.ui.view">
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_account_move_line_filter" />
        <field name="arch" type="xml">
            <field name="journal_id" position="after">
                <field name="make_ids" />
            </field>
            <filter name="groupby_date" position="after">
                <filter
          string="Make"
          name="groupby_make_literal"
          domain="[]"
          context="{'group_by':'make_literal'}"
        />
            </filter>
        </field>
    </record>
</odoo>
