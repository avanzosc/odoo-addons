- Add support for updating existing `product.supplierinfo` records
  instead of always creating new ones.
- Support for product variants (`product.product`) in addition to
  product templates.
- English column headers as an alternative to the current Spanish-only
  mapping.
