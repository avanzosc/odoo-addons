<odoo>
    <record id="view_move_form" model="ir.ui.view">
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <page name="other_info" position="after">
                <page string="IPNR" name="page_ipnr_weights"
                      attrs="{'invisible': [('move_type', 'not in', ['out_invoice', 'out_refund'])]}">
                    <group>
                        <field name="x_plastic_weight_non_recyclable_total" readonly="1"/>
                        <field name="x_plastic_weight_recyclable_total" readonly="1"/>
                    </group>
                </page>
            </page>
        </field>
    </record>

    <record id="action_compute_plastic_weight_totals" model="ir.actions.server">
        <field name="name">IPNR – Calculate plastic weights</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">records._compute_plastic_weight_totals()</field>
    </record>
</odoo>