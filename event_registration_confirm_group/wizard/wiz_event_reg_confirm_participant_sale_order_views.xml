<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="event_sale_registration_action.action_confirm_participant_sale_order_from_event_reg" model="ir.actions.act_window">
        <field name="name">Confirm course participant registration and sale order</field>
        <field name="res_model">wiz.event.reg.confirm.participant.sale.order</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="event.model_event_registration" />
        <field name="groups_id" eval="[(4, ref('event_registration_confirm_group.group_confirm_event_participants'))]"/>
    </record>
</odoo>
