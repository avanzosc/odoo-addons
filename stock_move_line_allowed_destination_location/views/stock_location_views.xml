<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_location_tree2" model="ir.ui.view">
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_tree2" />
        <field name="arch" type="xml">
            <field name="usage" position="after">
                <field name="not_allowed_as_destination" optional="show" />
            </field>
        </field>
    </record>

    <record id="view_location_form" model="ir.ui.view">
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form" />
        <field name="arch" type="xml">
            <field name="usage" position="after">
                <field name="not_allowed_as_destination" />
            </field>
        </field>
    </record>

    <record id="view_location_search" model="ir.ui.view">
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_search" />
        <field name="arch" type="xml">
            <field name="location_id" position="after">
                <separator />
                <filter
          string="Do Not Allow Move Lines At Destination"
          name="not-move-lines-in-destination"
          domain="[('not_allowed_as_destination','=',True)]"
        />
                <filter
          string="Allow Move Lines At Destination"
          name="move-lines-in-destination"
          domain="[('not_allowed_as_destination','=',False)]"
        />
            </field>
        </field>
    </record>
</odoo>
