<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record model="ir.ui.view" id="account_invoice_line_view_tree" >
        <field name="model">account.invoice.line</field>
        <field name="inherit_id" ref="account.view_invoice_line_tree"/>
        <field name="arch" type="xml">
            <field name="account_id" position="after">
                <field name="allowed_task_ids" invisible="1"/>
                <field name="task_id"
                       domain="[('id' ,'in', allowed_task_ids)]"/>
            </field>
        </field>
    </record>

    <record model="ir.ui.view" id="account_invoice_line_view_form" >
        <field name="model">account.invoice.line</field>
        <field name="inherit_id" ref="account.view_invoice_line_form"/>
        <field name="arch" type="xml">
            <field name="analytic_tag_ids" position="before">
                <field name="allowed_task_ids" invisible="1"/>
                <field name="task_id"
                       domain="[('id' ,'in', allowed_task_ids)]"/>
            </field>
        </field>
    </record>
</odoo>
